# macOS
.DS_Store

# Never commit a copy of the Skitch app if one is dropped here for reference
Skitch.app/

# SwiftPM / build artifacts
.build/
build/

# Written into the cwd by the LiveDoc MCP channel watcher, not part of Masume
channel.log

# MCP server build output and dependencies
mcp/node_modules/
mcp/dist/

# Compiler intermediates that a misconfigured build can drop into the root
/*.d
/*.dia
/*.swiftdeps
/*.swiftmodule

# Internal notes, not for the public repo
docs/internal/
